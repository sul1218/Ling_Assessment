import {all} from 'redux-saga/effects';
// import {watchNotifications} from '../../screens/notifications/redux/notifications.saga';

export function* rootSaga() {
  // yield all([watchNotifications()]);
}
